# Walkthrough — Bug Fix & Cleanup Scalp BTC Bot

## Ringkasan

Dilakukan perbaikan **15 bug/error logika** dan **12 item perapihan** pada proyek Scalp BTC Bot. Semua file Python telah diverifikasi lolos syntax check (AST parse).

---

## Perubahan per File

### File Baru

| File | Tujuan |
|------|--------|
| [.gitignore](file:///e:/Project/scalp_BTC_Bot/.gitignore) | Mencegah `.env`, `__pycache__`, `data/`, `logs/` masuk Git |
| [.env.example](file:///e:/Project/scalp_BTC_Bot/.env.example) | Template environment variables tanpa data sensitif |
| [src/__init__.py](file:///e:/Project/scalp_BTC_Bot/src/__init__.py) | Package init untuk `src` |
| [config/__init__.py](file:///e:/Project/scalp_BTC_Bot/config/__init__.py) | Package init untuk `config` |

### File yang Dihapus

| File | Alasan |
|------|--------|
| `deepseek_answer.txt` | File scratch debug yang tidak relevan untuk repo |

---

### [main.py](file:///e:/Project/scalp_BTC_Bot/main.py)

| Bug | Perbaikan |
|-----|-----------|
| BUG-02 | Satu `AsyncClient` bersama yang di-inject ke `MarketStream` dan `LiveTrader` |
| BUG-04 | Cooldown hanya reset saat transisi posisi (close → open), menggunakan flag `_was_in_position` |
| BUG-05 | Kill switch reset harian otomatis saat 00:00 UTC |
| BUG-09 | Flow ghost signal terpisah dengan early return — AI hanya dipanggil saat ada sinyal |

---

### [market_stream.py](file:///e:/Project/scalp_BTC_Bot/src/market_stream.py)

| Bug | Perbaikan |
|-----|-----------|
| BUG-01 | Method baru `_update_or_append_live()` — terapkan `MAX_KLINES` juga untuk candle yang belum tertutup |
| BUG-02 | Menerima `AsyncClient` via constructor injection |

---

### [live_trader.py](file:///e:/Project/scalp_BTC_Bot/src/live_trader.py)

| Bug | Perbaikan |
|-----|-----------|
| BUG-05 | Flag `is_killed` persisten — sekali aktif, bot berhenti trading sisa hari. `notify_kill_switch()` sekarang dipanggil |
| BUG-06 | **Chasing Limit Order**: offset harga progresif 0.03% per percobaan, hingga 3x percobaan (5 detik/percobaan) |
| BUG-15 | `notify_kill_switch()` diintegrasikan ke `check_kill_switch()` |
| BUG-02 | Menerima `AsyncClient` via constructor injection |

---

### [strategy.py](file:///e:/Project/scalp_BTC_Bot/src/strategy.py)

| Bug | Perbaikan |
|-----|-----------|
| BUG-07 | `datetime.datetime.utcnow()` → `datetime.now(timezone.utc)` (2 lokasi) |
| BUG-08 | VWAP: ganti merge → groupby/cumsum langsung pada `df_5m` (mencegah duplicate rows) |

---

### [ai_analyzer.py](file:///e:/Project/scalp_BTC_Bot/src/ai_analyzer.py)

| Perubahan | Detail |
|-----------|--------|
| Timeout | 30 detik per request API |
| Retry | Hingga 2 percobaan, gagal total = tolak sinyal (fail-safe) |
| Log | Semua pesan dalam Bahasa Indonesia |

---

### [notifier.py](file:///e:/Project/scalp_BTC_Bot/src/notifier.py)

| Bug | Perbaikan |
|-----|-----------|
| BUG-14 | Ellipsis `...` hanya ditambahkan jika teks > 200 karakter |
| Cleanup | Hapus `import asyncio` yang tidak digunakan, log ke Bahasa Indonesia |

---

### [engine.py](file:///e:/Project/scalp_BTC_Bot/src/backtester/engine.py) (Backtester)

| Bug | Perbaikan |
|-----|-----------|
| BUG-03 | `asyncio.run()` → `loop.run_until_complete()` dengan pembuatan event loop yang aman |
| BUG-10 | Flag `in_position` di-set `True` saat trade masuk, `False` setelah keluar (mencegah trade overlap) |

---

### Cleanup

| File | Perubahan |
|------|-----------|
| [requirements.txt](file:///e:/Project/scalp_BTC_Bot/requirements.txt) | Hapus `sqlalchemy` & `aiosqlite` (unused), tambah `numpy`, perbaiki version pinning |
| [docker-compose.yml](file:///e:/Project/scalp_BTC_Bot/docker-compose.yml) | Hapus deprecated `version: '3.8'` |
| [README.md](file:///e:/Project/scalp_BTC_Bot/README.md) | Rewrite total — sesuai arsitektur aktual (EMA 200, 5m, scoring system, chasing order) |

---

## Verifikasi

| Test | Hasil |
|------|-------|
| AST syntax check 10 file Python | ✅ Semua valid |
| docker-compose.yml readable | ✅ OK |

> [!IMPORTANT]
> **Pengingat keamanan**: File `.env` masih berisi API key asli. Segera rotasi semua API key (Binance, Telegram, DeepSeek) karena kemungkinan sudah terekspos di Git history.
